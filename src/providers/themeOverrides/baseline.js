import { palette } from 'src/colors';

export const MuiCssBaseline = {
  styleOverrides: {
    body: {
      fontSize: '0.875rem',
      backgroundColor: palette.background,
      whiteSpace: 'initial'
    }
  }
};
